<footer class="footer">
    <div class="container">
        <div class="row">
            <p class="col-12 my-3 text-center m-0">{{ __('ui.copyright') }}</p>
            <div class="col-12 text-center">
                <h5>{{ __('ui.become_revisor_question') }}</h5>
                <p>{{ __('ui.become_revisor_description') }}</p>
                <a href="{{ route('become.revisor') }}" class="btn btn-success mb-2">{{ __('ui.become_revisor') }}</a>
            </div>   
        </div>
    </div>
</footer>